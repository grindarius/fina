/** A RegExp pattern that checks for any positive and negative floats and integers. */
export const validateNumberRegExp: RegExp = /^-?(0|[1-9]\d*)(\.\d+)?$/

/**
 * A RegExp pattern that detect every dot in the string.
 */
export const dotRegExp: RegExp = /\./g
